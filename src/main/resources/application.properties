server.port=${env.VERI_CONTROLLER_PORT}
server.ssl.enabled=${env.VERI_CONTROLLER_SSL_ENABLED}
server.ssl.key-store=${env.VERI_CONTROLLER_SSL_KEYSTORE}
server.ssl.key-store-password=${env.VERI_CONTROLLER_SSL_KEYSTORE_PASSWORD}
server.ssl.key-store-type=${env.VERI_CONTROLLER_SSL_KEYSTORE_TYPE}
server.ssl.key-alias=${env.VERI_CONTROLLER_SSL_KEY_ALIAS}
mongodb.host=${env.VERI_MONGODB_HOST}
mongodb.port=${env.VERI_MONGODB_PORT}
mongodb.username=${env.VERI_MONGODB_USERNAME}
mongodb.password=${env.VERI_MONGODB_PASSWORD}
mongodb.authentication-database=${env.VERI_MONGODB_AUTH_DB_NAME}
mongodb.database=${env.VERI_MONGODB_DB_NAME}
mongodb.ssl.enabled=${env.VERI_MONGODB_SSL_ENABLED}
mongodb.ssl.key-store=${env.VERI_MONGODB_SSL_KEYSTORE}
mongodb.ssl.key-store-password=${env.VERI_MONGODB_SSL_KEYSTORE_PASSWORD}
mongodb.ssl.trust-store=${env.VERI_MONGODB_SSL_TRUSTSTORE}
mongodb.ssl.trust-store-password=${env.VERI_MONGODB_SSL_TRUSTSTORE_PASSWORD}
mongodb.ssl.invalid-hostname-allowed=${env.VERI_MONGODB_SSL_INVALID_HOSTNAME_ALLOWED}
server.error.include-stacktrace=on_param
spring.messages.basename=i18n/messages
verification.ui.hostA=${env.VERI_ALLOWED_ORIGIN_HOST_A}
verification.ui.hostB=${env.VERI_ALLOWED_ORIGIN_HOST_B}
verification.ui.hostC=${env.VERI_ALLOWED_ORIGIN_HOST_C}
verification.agent.name=${env.VERI_AGENT_NAME}
verification.agent.host=${env.VERI_AGENT_API_URL}
verification.agent.port=${env.VERI_AGENT_PORT_ADMIN}
verification.agent.endpoint=${env.VERI_AGENT_ENDPOINT}
verification.agent.api_key=${env.VERI_AGENT_API_KEY}
verification.agent.webhook.api_key=${env.VERI_AGENT_WEBHOOK_API_KEY}
verification.agent.api_key_header_name=${env.AGENT_API_KEY_HEADER_NAME}
verification.agent.didcomm_url=${env.AGENT_DIDCOMM_URL}
verification.agent.aries_message_type=${env.AGENT_ARIES_MESSAGE_TYPE}
verification.agent.aries_attach_id=${env.AGENT_ARIES_ATTACH_ID}
verification.agent.verkey=${env.VERI_AGENT_VERKEY}
verification.credentials.employee.credential_definition_id=${env.EMPLOYEE_CREDENTIAL_DEFINITION}
verification.credentials.employee.schema_id=${env.EMPLOYEE_CREDENTIAL_SCHEMA}
verification.credentials.guest.credential_definition_id=${env.GUEST_CREDENTIAL_DEFINITION}
verification.credentials.guest.schema_id=${env.GUEST_CREDENTIAL_SCHEMA}
bdr.credentials.basis_id.credential_definition_id=${env.BDR_BASIS_ID_CREDENTIAL_DEFINITION}
bdr.credentials.basis_id.employee.schema_id=${env.BDR_BASIS_ID_SCHEMA}
verification.info.title=${env.VERI_INFO_TITLE}
verification.info.description=${env.VERI_INFO_DESCRIPTION}
verification.info.version=${env.VERI_INFO_VERSION}
verification.info.contact.name=${env.VERI_INFO_CONTACT_NAME}
verification.info.contact.url=${env.VERI_INFO_CONTACT_URL}
verification.info.contact.email=${env.VERI_INFO_CONTACT_EMAIL}
id_provider.host=${env.VERI_ID_PROVIDER_HOST}
id_provider.permissions.path=${env.VERI_ID_PROVIDER_PERMISSIONS_PATH}
id_provider.token.path=${env.VERI_ID_PROVIDER_TOKEN_PATH}
id_provider.port=${env.VERI_ID_PROVIDER_PORT}
id_provider.realm=${env.VERI_ID_PROVIDER_REALM}
id_provider.client_id=${env.VERI_ID_PROVIDER_VERI_CLIENT_ID}
id_provider.client_secret=${env.VERI_ID_PROVIDER_VERI_CLIENT_SECRET}
id_provider.ssl.trust_all=${env.VERI_ID_PROVIDER_SSL_TRUST_ALL}
id_provider.ssl.verify_hostname=${env.VERI_ID_PROVIDER_VERIFY_HOSTNAME}
accreditation.api.url=${env.ACCR_API_URL}
accreditation.api.api_key=${env.ACCR_API_KEY}
accreditation.api.api_key_header_name=${env.ACCR_API_KEY_HEADER_NAME}
accreditation.api.ssl.trust_all=${env.VERI_ACCR_API_SSL_TRUST_ALL}
accreditation.api.ssl.verify_hostname=${env.VERI_ACCR_API_SSL_VERIFY_HOSTNAME}
guest.basisIdVerificationDevMode=${env.VERI_BASIS_ID_VERIFICATION_DEV_MODE}
guest.checkoutDelay=${env.VERI_CHECKOUT_DELAY_IN_SECONDS}
guest.verificationTimeout=${env.VERI_VERIFICATION_TIMEOUT_IN_SECONDS}
swagger_ui.id_provider.host=${env.VERI_SWAGGER_UI_ID_PROVIDER_HOST}
verification.ui.i18n.path=${env.VERI_I18N_PATH}
scheduler.poolSize=${env.VERI_SCHEDULER_POOLSIZE}
